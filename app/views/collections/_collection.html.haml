.product
  = link_to collection_path(collection) do
    = image_tag collection.featured_image
    .product-description
      %span.name
        = collection.name
      %br/
      = collection.description
      %br/
      = "#{collection.cart_items_count} artworks"
